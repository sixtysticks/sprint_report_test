<p id="notice"><%= notice %></p>

<h2>
	Sprint
	<%= @sprint.name_sprint %>
</h2>

<p>
	<strong>From:</strong>
	<%= @sprint.from %>
</p>

<p>
	<strong>To:</strong>
	<%= @sprint.to %>
</p>

<p>
	<strong>Total days:</strong>
	<%= @sprint.total_days %>
</p>
<br>
<p><%= link_to 'Export to PDF', sprint_path(@sprint, :format => 'pdf') %></p>

<br>
<%= link_to 'Edit', edit_sprint_path(@sprint) %> |
<%= link_to 'Back', sprints_path %>
